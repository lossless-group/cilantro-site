---
// Solution--Default.astro
// Ported from React to Astro with CSS-only motion, no external libraries.
// Implements section background, animated header, features grid with hover effects.
---
<section id="solution" class="solution" aria-label="Solution">
  <!-- Background gradient and glows -->
  <div class="bg-base" aria-hidden="true"></div>
  <div class="bg-glow" aria-hidden="true">
    <div class="glow glow-a"></div>
    <div class="glow glow-b"></div>
  </div>

  <div class="container">
    <!-- Section header -->
    <div class="header reveal" style="--delay: 0s">
      <h2 class="title">
        The <span class="accent">Parslee</span> Advantage
      </h2>
      <p class="subtitle">
        Built specifically for business documents, designed for enterprise teams who need AI they can trust.
      </p>
    </div>

    <!-- Features grid -->
    <div class="grid">
      <!-- Feature: Built for Microsoft 365 -->
      <div class="card reveal" style="--delay: 0.1s">
        <div class="icon-wrap">
          <div class="icon-grad">
            <svg class="icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="3" y="11" width="18" height="10" rx="2" />
              <rect x="7" y="3" width="10" height="8" rx="2" />
              <path d="M7 11v-2" />
              <path d="M17 11v-2" />
            </svg>
          </div>
        </div>
        <div class="content">
          <h3 class="card-title">Built for Microsoft 365</h3>
          <p class="card-copy">Integrates directly with SharePoint, OneDrive, and Teams. Respects file permissions and indexes only what matters.</p>
        </div>
        <div class="hover-glow" aria-hidden="true"></div>
      </div>

      <!-- Feature: Answers You Can Trust -->
      <div class="card reveal" style="--delay: 0.2s">
        <div class="icon-wrap">
          <div class="icon-grad">
            <svg class="icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M12 2l3 7 7 1-5 5 2 8-7-4-7 4 2-8-5-5 7-1z" />
              <path d="M12 9v6" />
              <circle cx="12" cy="17" r="1" />
            </svg>
          </div>
        </div>
        <div class="content">
          <h3 class="card-title">Answers You Can Trust</h3>
          <p class="card-copy">Line-level citations, instant previews, and structured outputs you can verify.</p>
        </div>
        <div class="hover-glow" aria-hidden="true"></div>
      </div>

      <!-- Feature: Enterprise-Grade from Day One -->
      <div class="card reveal" style="--delay: 0.3s">
        <div class="icon-wrap">
          <div class="icon-grad">
            <svg class="icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="3" y="11" width="18" height="10" rx="2" />
              <path d="M12 11V4" />
              <path d="M8 8h8" />
            </svg>
          </div>
        </div>
        <div class="content">
          <h3 class="card-title">Enterprise-Grade from Day One</h3>
          <p class="card-copy">Designed with security, scalability, and user permissions in mind.</p>
        </div>
        <div class="hover-glow" aria-hidden="true"></div>
      </div>

      <!-- Feature: Developer API Coming Soon -->
      <div class="card reveal" style="--delay: 0.4s">
        <div class="icon-wrap">
          <div class="icon-grad">
            <svg class="icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M16 18l6-6-6-6" />
              <path d="M8 6l-6 6 6 6" />
              <path d="M12 12h0" />
            </svg>
          </div>
        </div>
        <div class="content">
          <h3 class="card-title">Developer API Coming Soon</h3>
          <p class="card-copy">A powerful API will let teams build workflows and apps directly on top of Parslee's document intelligence engine.</p>
        </div>
        <div class="hover-glow" aria-hidden="true"></div>
      </div>

      <!-- Feature: Magnitude Better -->
      <div class="card reveal" style="--delay: 0.5s">
        <div class="icon-wrap">
          <div class="icon-grad">
            <svg class="icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M3 17l5-5 4 4 7-7" />
              <path d="M3 3v18h18" />
            </svg>
          </div>
        </div>
        <div class="content">
          <h3 class="card-title">Magnitude Better</h3>
          <p class="card-copy">Order-of-magnitude improvements in speed, usability, and data reliability compared to generic AI tools.</p>
        </div>
        <div class="hover-glow" aria-hidden="true"></div>
      </div>
    </div>

    <!-- Bottom CTA hint -->
    <div class="cta reveal" style="--delay: 0.8s">
      <p class="cta-copy">Ready to see the difference? Join our beta program below.</p>
    </div>
  </div>
</section>

<style>
  .solution {
    /* Use site tokens and brand vars */
    position: relative;
    background: var(--background);
    overflow: hidden;
    padding: 96px 0;
  }
  @media (min-width: 1024px) { .solution { padding: 128px 0; } }
  .bg-base { position: absolute; inset: 0; background-color: var(--background); }
  /* Disable glows to avoid washing out the background */
  .bg-glow { display: none; }
  .glow { position: absolute; width: 24rem; height: 24rem; border-radius: 999px; filter: blur(48px); }
  .glow-a { top: 25%; left: 20%; background: color-mix(in oklab, var(--accent) 12%, transparent); }
  .glow-b { bottom: 20%; right: 20%; background: color-mix(in oklab, var(--secondary) 12%, transparent); }

  .container { position: relative; max-width: 1120px; margin: 0 auto; padding: 0 24px; }
  .header { text-align: center; margin-bottom: 80px; }
  .title { font-family: var(--font-sans); font-size: clamp(2rem, 4vw, 3.5rem); color: var(--foreground); margin: 0 0 16px; }
  .accent { color: var(--secondary); }
  .subtitle { font-family: var(--font-sans); font-size: clamp(1.125rem, 2.2vw, 1.25rem); color: var(--muted-foreground); max-width: 56ch; margin: 0 auto; }

  .grid { display: grid; grid-template-columns: 1fr; gap: 24px; }
  @media (min-width: 768px) { .grid { grid-template-columns: 1fr 1fr; } }
  @media (min-width: 1024px) { .grid { grid-template-columns: 1fr 1fr 1fr; } }

  .card { position: relative; background: color-mix(in oklab, var(--card) 94%, transparent); backdrop-filter: blur(8px); border: 1px solid var(--border); border-radius: 20px; padding: 24px; transition: background 300ms ease, transform 200ms ease; }
  .card:hover { background: color-mix(in oklab, var(--card) 97%, transparent); transform: translateY(-2px); }

  .icon-wrap { margin-bottom: 20px; }
  .icon-grad { width: 64px; height: 64px; border-radius: 16px; display: flex; align-items: center; justify-content: center; background-image: linear-gradient(135deg, color-mix(in oklab, var(--accent) 12%, transparent), color-mix(in oklab, var(--secondary) 12%, transparent)); transition: transform 300ms ease; }
  .card:hover .icon-grad { transform: scale(1.08); }
  .icon { display: block; }
  /* Drive icon contrast via tokens */
  .icon { color: var(--foreground); }
  [data-theme="dark"] .solution .icon, .dark .solution .icon { color: var(--accent-foreground); }

  .content { display: grid; gap: 12px; }
  .card-title { font-family: var(--font-sans); font-size: var(--text-xl); color: var(--foreground); }
  .card-copy { font-family: var(--font-sans); color: var(--muted-foreground); line-height: 1.6; }

  .hover-glow { position: absolute; inset: 0; border-radius: 20px; background-image: linear-gradient(90deg, color-mix(in oklab, var(--accent) 14%, transparent), color-mix(in oklab, var(--secondary) 14%, transparent)); opacity: 0; transition: opacity 300ms ease; filter: blur(12px); z-index: -1; }
  .card:hover .hover-glow { opacity: 1; }

  .cta { text-align: center; margin-top: 48px; }
  .cta-copy { font-family: var(--font-sans); color: color-mix(in oklab, var(--accent-foreground) 60%, transparent); font-style: italic; }

  /* Reveal on scroll */
  .reveal { opacity: 0; transform: translateY(24px); transition: opacity 600ms ease, transform 600ms ease; transition-delay: var(--delay, 0s); }
  .reveal.in { opacity: 1; transform: translateY(0); }
</style>

<script type="module">
  function onReady(fn) {
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', fn);
    } else {
      fn();
    }
  }

  onReady(() => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('in');
        }
      });
    }, { rootMargin: '-100px 0px', threshold: 0.1 });

    document.querySelectorAll('.reveal').forEach((el) => observer.observe(el));
  });
</script>