---
---
<section class="neo-hero" aria-label="Neo Hero">
  <div class="neo-hero__bg-effects">
    <div class="neo-hero__gradient"></div>
    <div class="neo-hero__grid"></div>
  </div>

  <div class="neo-hero__container">
    <!-- Announcement Badge -->
    <div class="neo-hero__announcement">
      <span class="announcement-badge">
        <span class="announcement-dot"></span>
        We're Open Sourcing our favorite Internal Tool
      </span>
    </div>

    <!-- Hero Content -->
    <div class="neo-hero__content">
      <img class="neo-hero__icon" src="/appIcon__Neo.webp" alt="Neo App Icon" width="160" height="160" />

      <h1 class="neo-hero__title">
        The missing context layer for real Code Assistant workloads
      </h1>

      <p class="neo-hero__description">
        Neo is a self-improving code reasoning engine with persistent semantic memory, using multi-agent reasoning to analyze code, generate solutions, and continuously improve through feedback loops.
      </p>

      <!-- Feature Cards -->
      <div class="neo-hero__features">
        <article class="feature-card">
          <svg class="feature-icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 2L2 7v10c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V7l-10-5z"/>
            <circle cx="12" cy="12" r="3"/>
          </svg>
          <h3 class="feature-title">Persistent Memory</h3>
          <p class="feature-description">Learns from every solution attempt</p>
        </article>

        <article class="feature-card">
          <svg class="feature-icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="11" cy="11" r="8"/>
            <path d="M21 21l-4.35-4.35"/>
            <circle cx="11" cy="11" r="3"/>
          </svg>
          <h3 class="feature-title">Semantic Retrieval</h3>
          <p class="feature-description">Vector search finds relevant patterns</p>
        </article>

        <article class="feature-card">
          <svg class="feature-icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="16 18 22 12 16 6"/>
            <polyline points="8 6 2 12 8 18"/>
            <line x1="12" y1="2" x2="12" y2="22"/>
          </svg>
          <h3 class="feature-title">Code-First Generation</h3>
          <p class="feature-description">No diff parsing failures</p>
        </article>

        <article class="feature-card">
          <svg class="feature-icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M13 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V9z"/>
            <polyline points="13 2 13 9 20 9"/>
            <rect x="8" y="13" width="2" height="6" rx="1"/>
            <rect x="12" y="13" width="2" height="6" rx="1"/>
          </svg>
          <h3 class="feature-title">Local Storage</h3>
          <p class="feature-description">Privacy-first JSON storage in ~/.neo directory</p>
        </article>

        <article class="feature-card">
          <svg class="feature-icon" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 2L2 7l10 5 10-5-10-5z"/>
            <path d="M2 17l10 5 10-5"/>
            <path d="M2 12l10 5 10-5"/>
          </svg>
          <h3 class="feature-title">Model-Agnostic</h3>
          <p class="feature-description">Works with any LM provider</p>
        </article>
      </div>
    </div>

    <!-- CTA Section -->
    <div class="neo-hero__cta-section">
      <div class="neo-hero__cta-content">
        <a href="/jam-with/neo" class="neo-hero__learn-more">
          Learn More
          <svg class="learn-more-arrow" viewBox="0 0 16 16" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M1 8h14M9 2l6 6-6 6"/>
          </svg>
        </a>

        <a href="https://github.com/Parslee-ai/neo" target="_blank" rel="noopener noreferrer" class="neo-hero__cta-primary">
          <svg class="cta-icon" viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">
            <path fill-rule="evenodd" clip-rule="evenodd" d="M12 2C6.477 2 2 6.477 2 12a10 10 0 0 0 6.838 9.49c.5.091.682-.217.682-.483 0-.237-.009-.866-.013-1.701-2.782.604-3.37-1.34-3.37-1.34-.455-1.157-1.11-1.466-1.11-1.466-.907-.62.069-.607.069-.607 1.003.071 1.53 1.03 1.53 1.03.892 1.528 2.341 1.087 2.91.831.091-.647.35-1.087.636-1.338-2.221-.253-4.555-1.111-4.555-4.944 0-1.091.39-1.984 1.029-2.682-.103-.253-.446-1.272.098-2.65 0 0 .84-.269 2.75 1.025A9.564 9.564 0 0 1 12 7.07c.851.004 1.707.115 2.507.337 1.909-1.294 2.748-1.025 2.748-1.025.546 1.378.203 2.397.1 2.65.64.698 1.028 1.592 1.028 2.682 0 3.842-2.338 4.687-4.566 4.936.359.31.679.922.679 1.858 0 1.34-.012 2.42-.012 2.75 0 .268.18.579.688.481A10 10 0 0 0 22 12c0-5.523-4.477-10-10-10Z" fill="currentColor"/>
          </svg>
          View on GitHub
        </a>
      </div>

      <div class="neo-hero__claude-showcase">
        <p class="neo-hero__cta-label">Also Available as a Claude Code Plugin</p>
        <img
          class="claude-banner"
          src="https://ik.imagekit.io/xvpgfijuw/parslee/bannerFor__Claude-Code--Smaller.webp?updatedAt=1760294516640"
          alt="Claude Code Plugin"
          width="400"
          height="200"
        />
      </div>
    </div>
  </div>

  <style>
    /* Container & Layout */
    .neo-hero {
      position: relative;
      background: linear-gradient(180deg, #0b0f0a 0%, #070a08 100%);
      color: #d6ffe8;
      padding: 5rem 1.5rem;
      min-height: 100vh;
      display: flex;
      align-items: center;
      overflow: hidden;
      --neo-green: #00ff9c;
    }

    /* Background Effects */
    .neo-hero__bg-effects {
      position: absolute;
      inset: 0;
      pointer-events: none;
    }

    .neo-hero__gradient {
      position: absolute;
      inset: 0;
      background: radial-gradient(1200px 800px at 50% 30%, rgba(0,255,156,0.12), transparent 70%);
    }

    .neo-hero__grid {
      position: absolute;
      inset: 0;
      background-image:
        linear-gradient(rgba(0,255,156,0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,255,156,0.04) 1px, transparent 1px);
      background-size: 40px 40px;
      mask-image: radial-gradient(1000px 700px at 50% 40%, rgba(0,0,0,0.5), transparent 85%);
    }

    .neo-hero__container {
      position: relative;
      max-width: 1000px;
      margin: 0 auto;
      z-index: 1;
    }

    /* Announcement Badge */
    .neo-hero__announcement {
      display: flex;
      justify-content: center;
      margin-bottom: 3rem;
    }

    .announcement-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.625rem;
      padding: 0.625rem 1.25rem;
      background: rgba(0,255,156,0.08);
      border: 1px solid rgba(0,255,156,0.2);
      border-radius: 100px;
      font-size: clamp(0.7rem, 2vw, 0.875rem);
      font-weight: 600;
      color: #c0ffe0;
      box-shadow: 0 4px 20px rgba(0,255,156,0.15);
      transition: all 0.3s ease;
    }

    .announcement-badge:hover {
      background: rgba(0,255,156,0.12);
      box-shadow: 0 6px 28px rgba(0,255,156,0.22);
    }

    .announcement-dot {
      display: inline-block;
      width: 6px;
      height: 6px;
      background: var(--neo-green);
      border-radius: 50%;
      box-shadow: 0 0 8px rgba(0,255,156,0.6);
      animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.6; transform: scale(0.9); }
    }

    /* Hero Content */
    .neo-hero__content {
      text-align: center;
      margin-bottom: 4rem;
    }

    .neo-hero__icon {
      width: 160px;
      height: 160px;
      border-radius: 32px;
      margin: 0 auto 2.5rem;
      filter: drop-shadow(0 0 40px rgba(0,255,156,0.5));
      transition: transform 0.3s ease, filter 0.3s ease;
    }

    .neo-hero__icon:hover {
      transform: scale(1.05);
      filter: drop-shadow(0 0 50px rgba(0,255,156,0.7));
    }

    .neo-hero__title {
      font-size: clamp(2rem, 5.5vw, 3.5rem);
      font-weight: 800;
      line-height: 1.15;
      margin: 0 auto 1.5rem;
      max-width: 20ch;
      background: linear-gradient(135deg, var(--neo-green) 0%, #d6ffe8 100%);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: -0.02em;
    }

    .neo-hero__description {
      font-size: clamp(1rem, 2vw, 1.25rem);
      line-height: 1.65;
      color: #b3ffdb;
      max-width: 52ch;
      margin: 0 auto 1.5rem;
    }

    /* Learn More Link */
    .neo-hero__learn-more {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 1rem;
      font-weight: 600;
      color: var(--neo-green);
      text-decoration: none;
      position: relative;
      padding: 0.5rem 1rem;
      margin-bottom: 1.5rem;
      overflow: hidden;
      transition: all 0.3s ease;
    }

    .neo-hero__learn-more::before {
      content: "";
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--neo-green), transparent);
      transform: scaleX(0);
      transition: transform 0.3s ease;
    }

    .neo-hero__learn-more::after {
      content: "";
      position: absolute;
      inset: 0;
      border-radius: 8px;
      padding: 1px;
      background: linear-gradient(90deg, transparent, var(--neo-green), transparent);
      -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
      mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
      -webkit-mask-composite: xor;
      mask-composite: exclude;
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .neo-hero__learn-more:hover {
      color: #d6ffe8;
      text-shadow: 0 0 12px rgba(0,255,156,0.5);
    }

    .neo-hero__learn-more:hover::before {
      transform: scaleX(1);
    }

    .neo-hero__learn-more:hover::after {
      opacity: 1;
    }

    .learn-more-arrow {
      color: var(--neo-green);
      transition: transform 0.3s ease;
    }

    .neo-hero__learn-more:hover .learn-more-arrow {
      transform: translateX(4px);
    }

    /* Feature Cards */
    .neo-hero__features {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(165px, 1fr));
      gap: 1rem;
      max-width: 1100px;
      margin: 0 auto;
      justify-content: center;
    }

    .feature-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 1.25rem 1rem;
      background: rgba(0,255,156,0.04);
      border: 1px solid rgba(0,255,156,0.15);
      border-radius: 16px;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .feature-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--neo-green), transparent);
      transform: translateX(-100%);
      transition: transform 0.5s ease;
    }

    .feature-card:hover {
      background: rgba(0,255,156,0.08);
      border-color: rgba(0,255,156,0.3);
      box-shadow: 0 10px 40px rgba(0,255,156,0.15);
      transform: translateY(-4px);
    }

    .feature-card:hover::before {
      transform: translateX(100%);
    }

    .feature-icon {
      color: var(--neo-green);
      margin-bottom: 0.75rem;
      filter: drop-shadow(0 0 8px rgba(0,255,156,0.4));
      transition: all 0.3s ease;
    }

    .feature-card:hover .feature-icon {
      filter: drop-shadow(0 0 12px rgba(0,255,156,0.6));
      transform: scale(1.1);
    }

    .feature-title {
      font-size: 0.875rem;
      font-weight: 700;
      color: var(--neo-green);
      margin: 0 0 0.5rem 0;
      line-height: 1.3;
    }

    .feature-description {
      font-size: 0.75rem;
      line-height: 1.4;
      color: #b3ffdb;
      margin: 0;
    }

    /* CTA Section */
    .neo-hero__cta-section {
      display: grid;
      grid-template-columns: 1fr;
      gap: 3rem;
      align-items: center;
      padding: 3rem 0 0;
      border-top: 1px solid rgba(0,255,156,0.1);
    }

    @media (min-width: 768px) {
      .neo-hero__cta-section {
        grid-template-columns: 1fr 1fr;
      }
    }

    .neo-hero__cta-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }

    @media (min-width: 768px) {
      .neo-hero__cta-content {
        align-items: flex-start;
      }
    }

    .neo-hero__cta-primary {
      display: inline-flex;
      align-items: center;
      gap: 0.75rem;
      padding: 1rem 2rem;
      background: var(--neo-green);
      color: #0b0f0a;
      font-size: 1.125rem;
      font-weight: 700;
      text-decoration: none;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0,255,156,0.3), inset 0 1px 0 rgba(255,255,255,0.2);
      transition: all 0.2s ease;
    }

    .neo-hero__cta-primary:hover {
      background: #d6ffe8;
      box-shadow: 0 14px 40px rgba(0,255,156,0.4), inset 0 1px 0 rgba(255,255,255,0.3);
      transform: translateY(-2px);
    }

    .cta-icon {
      filter: drop-shadow(0 1px 2px rgba(0,0,0,0.2));
    }

    .neo-hero__cta-label {
      font-size: 0.875rem;
      color: #b3ffdb;
      margin: 0 0 1rem 0;
      text-align: center;
    }

    .neo-hero__claude-showcase {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .claude-banner {
      width: 100%;
      max-width: 400px;
      height: auto;
      border-radius: 16px;
      border: 1px solid rgba(0,255,156,0.15);
      filter: drop-shadow(0 10px 40px rgba(0,255,156,0.25));
      transition: all 0.3s ease;
    }

    .claude-banner:hover {
      filter: drop-shadow(0 15px 50px rgba(0,255,156,0.35));
      transform: scale(1.02);
    }

    /* Responsive Adjustments */
    @media (max-width: 767px) {
      .neo-hero {
        padding: 3rem 1.25rem;
        min-height: auto;
      }

      .neo-hero__icon {
        width: 120px;
        height: 120px;
      }

      .neo-hero__features {
        grid-template-columns: 1fr;
        gap: 1rem;
      }

      .feature-card {
        padding: 1.5rem 1.25rem;
      }
    }
  </style>
</section>